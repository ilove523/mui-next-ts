import { createMuiTheme } from '@material-ui/core/styles';

const theme = createMuiTheme({
  palette: {
    primary: {
      main: '#1e88e5',
      dark: '#1976d2',
      light: '#2196f3',
      contrastText: '#fff',
    },
  },
});

export default theme;
